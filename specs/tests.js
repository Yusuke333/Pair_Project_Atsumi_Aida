describe("Gacha", () => {
  it("should be there", () => {
    console.log(typeof gacha);
    expect(typeof gacha).toBe("object");
  });

  it("should have a money button", () => {});

  it("should have a withdraw method", () => {});

  it("should have a deposit method", () => {});
});
